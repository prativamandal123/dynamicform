import { CaptureHtmlDirective } from './capture-html.directive';

describe('CaptureHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptureHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
